# Agent Engineering Simulator 开发计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零构建一个「失败驱动实验」的交互式教学 Web 应用：用户运行裸 Agent → 观察隐藏故障 → 注入 Engineering Pattern → 重跑实验看到成功率提升，覆盖 Harness / Loop / Graph 三阶段共 13 个场景，构建产物直接部署 GitHub Pages 可用。

**Architecture:** 纯前端静态 SPA，无后端。所有"实验"由客户端确定性模拟引擎驱动：种子随机数 + Monte Carlo 批量试验得出成功率指标，单次试验生成事件时间线用于可视化叙事。场景 = 数据（数值模型）+ 内容（双语案文案），全部类型化存储在 TS 文件中。进度、能力库存、语言、主题持久化在 localStorage。

**Tech Stack:** Vite 7 + React 19 + TypeScript 5 · react-router-dom 7（HashRouter）· zustand 5（persist）· Tailwind CSS 4（class 策略双主题）· Vitest 3 + @testing-library/react · GitHub Actions → GitHub Pages

## Global Constraints

- 部署目标：GitHub Pages 项目页（`https://<user>.github.io/<repo>/`），因此 Vite `base: './'`（相对路径，不依赖仓库名），路由必须用 **HashRouter**（Pages 无服务端回退，BrowserRouter 刷新会 404）。
- 无后端、无外部 API 调用；一切数据在构建产物内。禁止引入需要密钥的服务。
- 双语：所有面向用户的字符串（UI chrome + 场景内容）必须有 `en` / `zh` 两个版本，默认 `zh`。
- 双主题：dark / light，class 策略，默认跟随 `prefers-color-scheme`，用户选择持久化。
- 模拟引擎必须确定性：相同 seed + 相同配置 → 相同结果（测试依赖此性质）。
- 每个 Task 结束必须 `npm run test` 全绿 + `npm run build` 成功后 commit。
- Node 22（CI 与本地一致）。

---

## 总体文件结构

```
.github/workflows/deploy.yml      # Pages 部署流水线
index.html                        # Vite 入口
package.json  tsconfig.json  vite.config.ts
src/
  main.tsx                        # 挂载 + Providers
  App.tsx                         # HashRouter 路由表
  index.css                       # Tailwind v4 + 主题 token
  theme/ThemeProvider.tsx         # dark/light 切换
  i18n/I18nProvider.tsx           # Locale context + useT()
  i18n/uiStrings.ts               # UI chrome 双语文案
  content/schema.ts               # ScenarioDef / ScenarioContent / CapabilityId 等类型
  content/capabilities.ts         # 16 个能力目录（双语名 + 描述）
  content/scenarios/index.ts      # 13 个场景注册表（按 order 排序）
  content/scenarios/scenario001.ts … scenario013.ts
  engine/rng.ts                   # mulberry32 种子随机数
  engine/simulator.ts             # runTrial / runMonteCarlo
  engine/events.ts                # 单次试验 → 事件时间线
  engine/*.test.ts                # 引擎单测（TDD）
  state/progressStore.ts          # zustand persist：完成状态、能力库存、语言、主题
  components/Layout.tsx           # 顶栏（语言/主题切换）+ 容器
  components/StageMap.tsx         # 首页三阶段进化地图
  components/ExperimentShell.tsx  # 场景页核心：任务→运行→故障→注入→重跑→完成
  components/CapabilityPanel.tsx  # 能力开关面板
  components/EventTimeline.tsx    # 单次运行事件流
  components/MetricsPanel.tsx     # Monte Carlo 指标 + SuccessGauge
  components/SuccessGauge.tsx     # 成功率仪表（动画）
  components/PatternCard.tsx      # 完成后导出的 Pattern 卡片
  pages/HomePage.tsx  ScenarioPage.tsx  PatternsPage.tsx  AboutPage.tsx
```

---

## 核心数据模型（所有 Task 的契约，先读这里）

```ts
// src/content/schema.ts
export type StageId = 'harness' | 'loop' | 'graph';

export type CapabilityId =
  | 'context-injection' | 'tool-registry' | 'tool-contract' | 'retry-policy'
  | 'sandbox' | 'permission-gate' | 'checkpointing' | 'memory-management'
  | 'context-engineering' | 'observation-loop' | 'recovery-loop' | 'stop-rule'
  | 'evidence-loop' | 'budget-guard' | 'graph-orchestration' | 'human-gate';

export type FailureId =
  | 'hallucination' | 'tool-failure' | 'unsafe-execution' | 'permission-error'
  | 'state-corruption' | 'memory-failure' | 'context-overflow' | 'stale-context'
  | 'task-abandoned' | 'infinite-loop' | 'false-completion' | 'budget-exhausted'
  | 'deadlock';

export interface LocalizedText { en: string; zh: string; }

/** 数值模型：决定模拟结果（与文案分离） */
export interface ScenarioDef {
  id: string;                    // 'scenario-001'
  order: number;                 // 1..13，解锁顺序
  stage: StageId;
  hiddenFailure: FailureId;
  baseSuccess: number;           // 裸 Agent 成功率，如 0.08
  capabilityEffects: Partial<Record<CapabilityId, number>>; // 正确能力的加性提升
  requiredCapabilities: CapabilityId[];  // 通关条件：全部启用
  unlocks: CapabilityId[];       // 通关后进入全局库存
  baseTokenCost: number;         // 单次试验平均 token
  trials: number;                // Monte Carlo 次数，统一 200
}

/** 双语文案 */
export interface ScenarioContent {
  title: LocalizedText;
  mission: LocalizedText;            // 实验目标
  failureName: LocalizedText;        // 故障名，如 HALLUCINATION
  failureNarrative: LocalizedText;   // 故障发生时的叙事
  missingCapabilityHint: LocalizedText;
  explanation: LocalizedText;        // 学习点：为什么需要这个 Pattern
  patternName: LocalizedText;        // 导出 Pattern 名
  patternSummary: LocalizedText;
}

export interface Scenario { def: ScenarioDef; content: ScenarioContent; }
```

**成功率模型（确定性）：** `p = clamp01(baseSuccess + Σ capabilityEffects[cap]（已启用）)`。未在 `capabilityEffects` 中列出的能力效果为 0 —— 这是核心教学点：错误的 Pattern 修不好这个故障。通关判定：`requiredCapabilities` 全部启用且已执行一次 Monte Carlo。

---

## Milestone 0：脚手架与部署

### Task 1: 仓库清零 + 工程脚手架 + GitHub Pages 流水线

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`, `.github/workflows/deploy.yml`, `.gitignore`
- Modify: 工作区遗留删除（git status 中的 `D` 文件）

**Interfaces:**
- Produces: `npm run dev|build|test` 可用；push 到 `main` 自动部署 Pages。

- [ ] **Step 1: 提交遗留清理**

工作区已有大量未暂存删除（旧实现）。保留 `docs/`：

```bash
git add -A && git commit -m "chore: clear legacy implementation, keep docs"
```

- [ ] **Step 2: 写 `package.json`（完整内容）**

```json
{
  "name": "agent-engineering-simulator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.0",
    "zustand": "^5.0.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@testing-library/react": "^16.3.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.5.0",
    "jsdom": "^26.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "~5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
```

- [ ] **Step 3: 写 `vite.config.ts` / `tsconfig.json` / `index.html` / 入口文件**

`vite.config.ts`（`base: './'` 是 Pages 项目页的关键）：

```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  base: './',
  plugins: [react(), tailwindcss()],
  test: { environment: 'jsdom', globals: true },
});
```

`tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022", "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext", "moduleResolution": "bundler", "jsx": "react-jsx",
    "strict": true, "noUnusedLocals": true, "noUnusedParameters": true,
    "noEmit": true, "skipLibCheck": true, "types": ["vitest/globals"]
  },
  "include": ["src"]
}
```

`index.html`：`<div id="root">` + `<script type="module" src="/src/main.tsx">`，`<html lang="zh-CN">`，title「Agent Engineering Simulator」。

`src/index.css`：

```css
@import "tailwindcss";
@custom-variant dark (&:where(.dark, .dark *));
:root { color-scheme: light; }
.dark { color-scheme: dark; }
```

`src/main.tsx` / `src/App.tsx`：最小可渲染（App 先返回 `<h1>Agent Engineering Simulator</h1>`，Task 6 替换为路由）。

- [ ] **Step 4: 写 `.github/workflows/deploy.yml`（完整内容）**

```yaml
name: Deploy to GitHub Pages
on:
  push: { branches: [main] }
  workflow_dispatch:
permissions:
  pages: write
  id-token: write
concurrency: { group: pages, cancel-in-progress: true }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 5: 安装 + 验证 + 首次部署**

```bash
npm install
npm run build   # 期望：dist/ 生成成功
git add -A && git commit -m "feat: scaffold Vite+React+TS with Pages deploy pipeline"
git push origin main
```

然后在 GitHub 仓库 **Settings → Pages → Source 选 "GitHub Actions"**，等 workflow 变绿，访问 `https://<user>.github.io/<repo>/` 看到 h1 即完成。`.gitignore` 含 `node_modules`、`dist`。

---

## Milestone 1：基础设施与模拟引擎

### Task 2: 主题与 i18n 基础设施

**Files:**
- Create: `src/theme/ThemeProvider.tsx`, `src/i18n/I18nProvider.tsx`, `src/i18n/uiStrings.ts`, `src/i18n/I18nProvider.test.tsx`

**Interfaces:**
- Produces: `<ThemeProvider>`（挂 `<html class="dark">`）、`useTheme(): { theme, toggle }`；`<I18nProvider>`、`useT(): (key: UiKey) => string`、`useLocale(): { locale, setLocale }`、`pick(text: LocalizedText): string`。

- [ ] **Step 1: 写失败测试** — `I18nProvider.test.tsx`：默认 locale 为 `zh`；`setLocale('en')` 后 `t('runBaseline')` 返回英文；`pick({en:'a',zh:'b'})` 随 locale 切换。运行确认失败（模块不存在）。

- [ ] **Step 2: 实现 `uiStrings.ts`（完整 key 集合，Task 8/9 会用到，先定义全）**

```ts
import type { LocalizedText } from '../content/schema';
export const ui = {
  appTitle: { en: 'Agent Engineering Simulator', zh: 'Agent 工程模拟器' },
  stages: {
    harness: { en: 'Harness Engineering', zh: 'Harness 工程' },
    loop: { en: 'Loop Engineering', zh: 'Loop 工程' },
    graph: { en: 'Graph Engineering', zh: 'Graph 工程' },
  },
  stageTaglines: {
    harness: { en: 'Make the agent work', zh: '让 Agent 能工作' },
    loop: { en: 'Make the agent finish reliably', zh: '让 Agent 可靠完成' },
    graph: { en: 'Make the agent run complex flows', zh: '让 Agent 支撑复杂流程' },
  },
  runBaseline: { en: 'Run Baseline Experiment', zh: '运行基线实验' },
  runAgain: { en: 'Re-run with Capabilities', zh: '注入能力后重跑' },
  running: { en: 'Running…', zh: '实验运行中…' },
  failureRevealed: { en: 'Hidden Failure Revealed', zh: '发现隐藏故障' },
  missingCapability: { en: 'Missing Capability', zh: '缺失能力' },
  injectCapability: { en: 'Capability Panel', zh: '能力注入面板' },
  successRate: { en: 'Success Rate', zh: '成功率' },
  tokenCost: { en: 'Avg Token Cost', zh: '平均 Token 消耗' },
  trials: { en: 'trials', zh: '次试验' },
  complete: { en: 'Complete & Export Pattern', zh: '完成实验 · 导出 Pattern' },
  completed: { en: 'Completed', zh: '已完成' },
  locked: { en: 'Locked', zh: '未解锁' },
  unlockedPatterns: { en: 'Pattern Library', zh: 'Pattern 库' },
  mission: { en: 'Mission', zh: '实验目标' },
  explanation: { en: 'Why it works', zh: '设计原理' },
  baseline: { en: 'Baseline', zh: '基线' },
  improved: { en: 'With Capabilities', zh: '注入能力后' },
  nextScenario: { en: 'Next Scenario', zh: '下一个场景' },
  about: { en: 'About', zh: '关于' },
} satisfies Record<string, LocalizedText | Record<string, LocalizedText>>;
```

- [ ] **Step 3: 实现两个 Provider（完整代码）**

`I18nProvider.tsx`：

```tsx
import { createContext, useContext, useState, type ReactNode } from 'react';
import type { LocalizedText } from '../content/schema';

export type Locale = 'en' | 'zh';
const I18nCtx = createContext<{ locale: Locale; setLocale: (l: Locale) => void }>({
  locale: 'zh', setLocale: () => {},
});

export function I18nProvider({ children }: { children: ReactNode }) {
  const [locale, setLocale] = useState<Locale>(
    () => (localStorage.getItem('locale') as Locale) || 'zh',
  );
  const set = (l: Locale) => { setLocale(l); localStorage.setItem('locale', l); };
  return <I18nCtx.Provider value={{ locale, setLocale: set }}>{children}</I18nCtx.Provider>;
}

export const useLocale = () => useContext(I18nCtx);
export function usePick() {
  const { locale } = useLocale();
  return (t: LocalizedText) => t[locale];
}
```

`ThemeProvider.tsx`：同理，`theme: 'dark' | 'light'`，`useEffect` 中 `document.documentElement.classList.toggle('dark', theme === 'dark')`，初始值 `localStorage.theme ?? (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light')`。

- [ ] **Step 4: 跑测试全绿，commit** `feat: add theme and i18n providers`

### Task 3: 能力目录 `capabilities.ts`

**Files:** Create `src/content/schema.ts`（上文契约原样落盘）, `src/content/capabilities.ts`

**Interfaces:**
- Produces: `CAPABILITIES: Record<CapabilityId, { name: LocalizedText; desc: LocalizedText }>`。

- [ ] **Step 1: 落盘 `schema.ts`（复制"核心数据模型"整段）。**

- [ ] **Step 2: 写全 16 个能力（双语文案，完整数据）**

| CapabilityId | zh 名 / en 名 | 一句话定位（zh） |
|---|---|---|
| context-injection | 上下文注入 / Context Injection | 把真实世界信息喂给模型 |
| tool-registry | 工具注册表 / Tool Registry | 声明可用工具及其契约 |
| tool-contract | 工具契约 / Tool Contract | 参数校验与错误语义 |
| retry-policy | 重试策略 / Retry Policy | 指数退避 + 次数上限 |
| sandbox | 沙箱 / Sandbox | 隔离执行，限制爆炸半径 |
| permission-gate | 权限门 / Permission Gate | 最小权限 + 授权确认 |
| checkpointing | 检查点 / Checkpointing | 快照、回滚、断点续跑 |
| memory-management | 记忆管理 / Memory Management | 选择、压缩、校验记忆 |
| context-engineering | 上下文工程 / Context Engineering | 决定什么进 prompt、什么留外面 |
| observation-loop | 观察循环 / Observation Loop | 定期刷新外部状态 |
| recovery-loop | 恢复循环 / Recovery Loop | 失败→诊断→再尝试的任务级循环 |
| stop-rule | 停止规则 / Stop Rule | max steps / 进度检查 / 预算 |
| evidence-loop | 证据循环 / Evidence Loop | 用任务证据而非自信度决定停止 |
| budget-guard | 预算守卫 / Budget Guard | token/时间/金钱上限与升级机制 |
| graph-orchestration | 图编排 / Graph Orchestration | 节点、边、状态转移、恢复路径 |
| human-gate | 人工关卡 / Human Gate | 关键节点人工确认 |

- [ ] **Step 3: commit** `feat: add capability catalog and content schema`

### Task 4: 种子随机数 + Monte Carlo 引擎（TDD 核心）

**Files:**
- Create: `src/engine/rng.ts`, `src/engine/rng.test.ts`, `src/engine/simulator.ts`, `src/engine/simulator.test.ts`

**Interfaces:**
- Consumes: `ScenarioDef`（Task 3）。
- Produces（Task 5、8、9 依赖，签名固定）：

```ts
export type Rng = () => number; // [0,1)
export function mulberry32(seed: number): Rng;

export interface TrialInput { scenario: ScenarioDef; enabled: ReadonlySet<CapabilityId>; seed: number; }
export interface TrialResult {
  success: boolean; failure: FailureId | null;
  tokenCost: number; steps: number; seed: number;
}
export function successRateOf(scenario: ScenarioDef, enabled: ReadonlySet<CapabilityId>): number;
export function runTrial(input: TrialInput): TrialResult;

export interface MonteCarloSummary {
  trials: number; successes: number; successRate: number;
  avgTokenCost: number; failureBreakdown: Partial<Record<FailureId, number>>;
}
export function runMonteCarlo(scenario: ScenarioDef, enabled: ReadonlySet<CapabilityId>, seed: number): MonteCarloSummary;
```

- [ ] **Step 1: 写失败测试 `rng.test.ts`**

```ts
import { mulberry32 } from './rng';
test('deterministic for same seed', () => {
  const a = mulberry32(42), b = mulberry32(42);
  for (let i = 0; i < 100; i++) expect(a()).toBe(b());
});
test('different seeds diverge', () => {
  expect(mulberry32(1)()).not.toBe(mulberry32(2)());
});
test('output in [0,1)', () => {
  const r = mulberry32(7);
  for (let i = 0; i < 1000; i++) { const v = r(); expect(v).toBeGreaterThanOrEqual(0); expect(v).toBeLessThan(1); }
});
```

- [ ] **Step 2: 实现 `rng.ts`**

```ts
export type Rng = () => number;
export function mulberry32(seed: number): Rng {
  let a = seed >>> 0;
  return () => {
    a |= 0; a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
```

- [ ] **Step 3: 写失败测试 `simulator.test.ts`（用 Task 10 的 scenario-001 数据构造 fixture）**

```ts
const s001: ScenarioDef = {
  id: 'scenario-001', order: 1, stage: 'harness', hiddenFailure: 'hallucination',
  baseSuccess: 0.08,
  capabilityEffects: { 'context-injection': 0.22, 'tool-registry': 0.05 },
  requiredCapabilities: ['context-injection', 'tool-registry'],
  unlocks: ['context-injection', 'tool-registry'],
  baseTokenCost: 1800, trials: 200,
};

test('successRateOf is additive and clamped', () => {
  expect(successRateOf(s001, new Set())).toBeCloseTo(0.08);
  expect(successRateOf(s001, new Set(['context-injection']))).toBeCloseTo(0.30);
  expect(successRateOf(s001, new Set(['context-injection', 'tool-registry']))).toBeCloseTo(0.35);
});
test('irrelevant capability has zero effect', () => {
  expect(successRateOf(s001, new Set(['sandbox']))).toBeCloseTo(0.08);
});
test('monte carlo converges to configured rate', () => {
  const s = runMonteCarlo(s001, new Set(), 1);
  expect(s.trials).toBe(200);
  expect(Math.abs(s.successRate - 0.08)).toBeLessThan(0.06);
});
test('same seed same result', () => {
  expect(runTrial({ scenario: s001, enabled: new Set(), seed: 99 }))
    .toEqual(runTrial({ scenario: s001, enabled: new Set(), seed: 99 }));
});
test('failed trial carries the hidden failure id', () => {
  const r = runTrial({ scenario: s001, enabled: new Set(), seed: 5 });
  if (!r.success) expect(r.failure).toBe('hallucination');
});
```

- [ ] **Step 4: 实现 `simulator.ts`（完整代码）**

```ts
import { mulberry32 } from './rng';
import type { CapabilityId, FailureId, ScenarioDef } from '../content/schema';

export interface TrialInput { scenario: ScenarioDef; enabled: ReadonlySet<CapabilityId>; seed: number; }
export interface TrialResult { success: boolean; failure: FailureId | null; tokenCost: number; steps: number; seed: number; }

export function successRateOf(scenario: ScenarioDef, enabled: ReadonlySet<CapabilityId>): number {
  let p = scenario.baseSuccess;
  for (const [cap, w] of Object.entries(scenario.capabilityEffects))
    if (enabled.has(cap as CapabilityId)) p += w;
  return Math.min(1, Math.max(0, p));
}

export function runTrial({ scenario, enabled, seed }: TrialInput): TrialResult {
  const rng = mulberry32(seed);
  const p = successRateOf(scenario, enabled);
  const success = rng() < p;
  const tokenCost = Math.round(scenario.baseTokenCost * (0.85 + 0.3 * rng()) + enabled.size * 120);
  const steps = 4 + Math.floor(rng() * 4);
  return { success, failure: success ? null : scenario.hiddenFailure, tokenCost, steps, seed };
}

export interface MonteCarloSummary {
  trials: number; successes: number; successRate: number;
  avgTokenCost: number; failureBreakdown: Partial<Record<FailureId, number>>;
}

export function runMonteCarlo(scenario: ScenarioDef, enabled: ReadonlySet<CapabilityId>, seed: number): MonteCarloSummary {
  let successes = 0, tokens = 0;
  const failureBreakdown: Partial<Record<FailureId, number>> = {};
  for (let i = 0; i < scenario.trials; i++) {
    const r = runTrial({ scenario, enabled, seed: seed * 100003 + i });
    if (r.success) successes++;
    else failureBreakdown[r.failure!] = (failureBreakdown[r.failure!] ?? 0) + 1;
    tokens += r.tokenCost;
  }
  return {
    trials: scenario.trials, successes,
    successRate: successes / scenario.trials,
    avgTokenCost: Math.round(tokens / scenario.trials),
    failureBreakdown,
  };
}
```

- [ ] **Step 5: 跑测试全绿，commit** `feat: deterministic simulation engine with monte carlo`

### Task 5: 事件时间线生成器（TDD）

**Files:** Create `src/engine/events.ts`, `src/engine/events.test.ts`

**Interfaces:**
- Consumes: `runTrial` 的 `TrialResult` + `ScenarioDef`。
- Produces（Task 9 渲染依赖）：

```ts
export type EventKind = 'trigger' | 'thought' | 'action' | 'tool-call' | 'failure' | 'mitigation' | 'verdict';
export interface RunEvent { step: number; kind: EventKind; text: LocalizedText; }
export function buildTimeline(scenario: ScenarioDef, result: TrialResult, enabled: ReadonlySet<CapabilityId>): RunEvent[];
```

- [ ] **Step 1: 写失败测试**：(a) 时间线以 `trigger` 开始、`verdict` 结束；(b) 失败试验必含一个 `kind:'failure'` 且其 step < verdict.step；(c) 启用 required 能力且成功时含 `kind:'mitigation'`；(d) 事件 step 严格递增；(e) 每个事件 text 同时有 en/zh。

- [ ] **Step 2: 实现 `events.ts`** — 模板驱动，按 `hiddenFailure` 查表生成故障叙事，结构：

```ts
// 骨架（每个 FailureId 在 FAILURE_STORY 表中有 { setup, failure, mitigation } 三段双语文案）
const FAILURE_STORY: Record<FailureId, {
  setup: LocalizedText; failure: LocalizedText; mitigation: LocalizedText;
}> = { /* Task 10 内容任务填充全部 13 条，本 task 先填 hallucination 一条 */ };

export function buildTimeline(scenario, result, enabled): RunEvent[] {
  const story = FAILURE_STORY[scenario.hiddenFailure];
  const covered = scenario.requiredCapabilities.every(c => enabled.has(c));
  const events: RunEvent[] = [
    { step: 1, kind: 'trigger', text: { en: 'Task received', zh: '收到任务' } },
    { step: 2, kind: 'thought', text: story.setup },
    { step: 3, kind: 'action', text: { en: 'Agent acts on the task', zh: 'Agent 开始执行' } },
  ];
  if (!result.success) {
    events.push({ step: 4, kind: 'failure', text: story.failure });
  } else if (covered) {
    events.push({ step: 4, kind: 'mitigation', text: story.mitigation });
  }
  events.push({
    step: 5, kind: 'verdict',
    text: result.success ? { en: 'Task succeeded', zh: '任务成功' } : { en: 'Task failed', zh: '任务失败' },
  });
  return events;
}
```

（`steps` 字段控制中间 thought/action 的重复插值，实现时用 `result.steps` 循环插入，保持 step 递增即可。）

- [ ] **Step 3: 跑测试全绿，commit** `feat: run timeline event generator`

---

## Milestone 2：应用外壳与状态

### Task 6: 路由 + Layout + 首页 StageMap

**Files:**
- Create: `src/pages/HomePage.tsx`, `src/pages/ScenarioPage.tsx`（占位，Task 8 填充）, `src/pages/PatternsPage.tsx`, `src/pages/AboutPage.tsx`, `src/components/Layout.tsx`, `src/components/StageMap.tsx`
- Modify: `src/App.tsx`, `src/main.tsx`

**Interfaces:**
- Consumes: `usePick`, `useLocale`, `useTheme`（Task 2）；`SCENARIOS` 注册表（Task 10 才全量，本 task 用 `content/scenarios/index.ts` 中空数组+类型先通过编译，Task 10 回填）。
- Produces: 路由 `/`、`/scenario/:id`、`/patterns`、`/about`（HashRouter）。

- [ ] **Step 1: `App.tsx` 路由表**

```tsx
import { HashRouter, Routes, Route } from 'react-router-dom';
import Layout from './components/Layout';
import HomePage from './pages/HomePage';
import ScenarioPage from './pages/ScenarioPage';
import PatternsPage from './pages/PatternsPage';
import AboutPage from './pages/AboutPage';

export default function App() {
  return (
    <HashRouter>
      <Routes>
        <Route element={<Layout />}>
          <Route index element={<HomePage />} />
          <Route path="scenario/:id" element={<ScenarioPage />} />
          <Route path="patterns" element={<PatternsPage />} />
          <Route path="about" element={<AboutPage />} />
        </Route>
      </Routes>
    </HashRouter>
  );
}
```

`main.tsx`：`<I18nProvider><ThemeProvider><App /></ThemeProvider></I18nProvider>`。

- [ ] **Step 2: `Layout.tsx`** — 顶栏：appTitle（链回 `/`）、`/patterns` 链接、语言切换按钮（`中/EN`）、主题切换按钮（日/月 SVG 图标）；`<Outlet />` 包在 `max-w-5xl mx-auto px-4` 容器中；配色用 Tailwind：`bg-white text-zinc-900 dark:bg-zinc-950 dark:text-zinc-100 min-h-screen`。

- [ ] **Step 3: `StageMap.tsx` + `HomePage.tsx`** — 首页渲染设计文档的三阶段进化图：三个 stage 卡片纵向排列、箭头连接，每卡片显示 `stages[stage]` 名 + tagline + 该阶段场景列表（场景名 + 完成态 ✓ / 锁定 🔒 图标，来自 Task 7 的 store；本 task 先全部按"order===1 解锁"渲染）。卡片点击跳 `/scenario/:id`。视觉：stage 卡片用左边框色条区分（harness=sky、loop=amber、graph=violet）。

- [ ] **Step 4: 冒烟测试 `App.test.tsx`**：render `<App />`，断言首页出现「Harness 工程」；切 locale 后出现「Harness Engineering」。全绿后 commit `feat: app shell with stage map home`

### Task 7: 进度存储（zustand persist）

**Files:** Create `src/state/progressStore.ts`, `src/state/progressStore.test.ts`

**Interfaces:**
- Produces（Task 6/8/11 依赖）：

```ts
interface ProgressState {
  completed: string[];                       // scenario id
  inventory: CapabilityId[];                 // 已解锁能力库存
  isUnlocked: (s: ScenarioDef) => boolean;   // order===1 或前一个已完成
  isCompleted: (id: string) => boolean;
  completeScenario: (s: ScenarioDef) => void; // 记录完成 + 合并 unlocks 进 inventory（去重）
}
export const useProgress = create<ProgressState>()(persist(..., { name: 'aes-progress' }));
```

- [ ] **Step 1: 写失败测试**：初始 inventory 为空；`completeScenario(s001)` 后 `isCompleted('scenario-001')` 为真且 inventory 含 `context-injection`、`tool-registry`；重复 complete 不重复入库；`isUnlocked` 对 order=2 的场景在完成 001 前后分别为 false/true。

- [ ] **Step 2: 实现 store**（zustand `create` + `persist` middleware，`isUnlocked` 需要全量场景表查 order —— 从 `content/scenarios` 导入注册表按 order 找前驱）。

- [ ] **Step 3: 全绿，commit** `feat: progress store with capability inventory`

---

## Milestone 3：实验体验（核心教学循环）

### Task 8: ExperimentShell —— 场景页主交互

**Files:**
- Create: `src/components/ExperimentShell.tsx`, `src/components/CapabilityPanel.tsx`
- Modify: `src/pages/ScenarioPage.tsx`

**Interfaces:**
- Consumes: 全部前序接口。`ScenarioPage` 用 `useParams` 取 id → `SCENARIOS` 查找 → 未解锁重定向首页 → 渲染 `<ExperimentShell scenario={s} />`。
- Produces: 完整教学循环 UI。

- [ ] **Step 1: 交互流程（按此实现，状态机）**

```
idle → running(基线 Monte Carlo + 单次 timeline 动画) → failed(故障揭示面板)
     → 用户在 CapabilityPanel 勾选能力 → running(重跑) → reviewed
     → requiredCapabilities 全启用时「完成」按钮可用 → completed(展示 PatternCard + 下一关入口)
```

- [ ] **Step 2: `ExperimentShell.tsx` 结构（完整实现要点）**

```tsx
// 局部状态
const [enabled, setEnabled] = useState<Set<CapabilityId>>(new Set());
const [phase, setPhase] = useState<'idle' | 'running' | 'failed' | 'reviewed' | 'completed'>('idle');
const [summary, setSummary] = useState<MonteCarloSummary | null>(null);
const [timeline, setTimeline] = useState<RunEvent[]>([]);
const [baselineSummary, setBaselineSummary] = useState<MonteCarloSummary | null>(null);

function run() {
  setPhase('running');
  const seed = Date.now() % 1_000_000;
  const mc = runMonteCarlo(scenario.def, enabled, seed);
  const trial = runTrial({ scenario: scenario.def, enabled, seed });
  setSummary(mc);
  if (!baselineSummary && enabled.size === 0) setBaselineSummary(mc);
  setTimeline(buildTimeline(scenario.def, trial, enabled));
  setPhase(trial.success || enabled.size > 0 ? 'reviewed' : 'failed');
}
const canComplete = scenario.def.requiredCapabilities.every(c => enabled.has(c)) && summary !== null;
```

布局：上=任务卡（mission + baseline 描述）；左=`CapabilityPanel`（仅显示 inventory 中能力 + 本场景 required 但尚未解锁的显示为"缺失"提示）；右=`MetricsPanel` + `EventTimeline`；底部=运行按钮 + 完成按钮。完成时调 `useProgress.completeScenario(scenario.def)` 并展示 `PatternCard`。

- [ ] **Step 3: `CapabilityPanel.tsx`** — 能力开关列表：每个能力 = checkbox + 双语名 + 一句话描述；未解锁能力置灰并带「完成前置场景解锁」提示。教学关键点：用户可勾选"错误"能力，重跑后成功率不变 —— 面板不做任何正确性提示。

- [ ] **Step 4: 组件测试**：render scenario-001 的 shell → 点「运行基线实验」→ 断言出现故障面板与「缺失能力」提示。全绿，commit `feat: experiment shell with capability injection loop`

### Task 9: 可视化组件（Timeline / Metrics / Gauge）

**Files:** Create `src/components/EventTimeline.tsx`, `src/components/MetricsPanel.tsx`, `src/components/SuccessGauge.tsx`, `src/components/PatternCard.tsx`

- [ ] **Step 1: `EventTimeline.tsx`** — 垂直时间线，按 `kind` 着色（failure=red、mitigation=emerald、verdict=粗体）；用 `useEffect` + `setInterval(400ms)` 逐条 reveal（运行叙事感），reveal 完回调父组件。每条事件左侧为 step 序号圆点。

- [ ] **Step 2: `SuccessGauge.tsx`** — SVG 半圆仪表，`strokeDasharray` 动画从旧值过渡到新值（`transition: stroke-dashoffset 0.8s`），中央大字号百分比。props: `{ value: number; label: string }`。

- [ ] **Step 3: `MetricsPanel.tsx`** — 两个 `SuccessGauge` 并排对比（baseline vs 当前配置，`baselineSummary` 存在时），下方 avgTokenCost 与 trials 文案，failureBreakdown 以横向条形列出（仅失败配置时显示）。

- [ ] **Step 4: `PatternCard.tsx`** — 完成后导出卡：patternName + patternSummary + 该场景 metrics 前后对比，样式为带左边框色条的卡片（stage 同色）。

- [ ] **Step 5: 渲染测试**（gauge 显示百分比、timeline 渲染全部事件）全绿，commit `feat: run visualizations and pattern card`

### Task 10: 13 个场景全量内容（双语）

**Files:** Create `src/content/scenarios/scenario001.ts` … `scenario013.ts`, `src/content/scenarios/index.ts`；Modify `src/engine/events.ts`（补全 `FAILURE_STORY` 13 条）

**Interfaces:**
- Consumes: schema、catalog、events 表结构。
- Produces: `SCENARIOS: Scenario[]`（按 order 升序，长度 13，导出 `getScenario(id)`）。

- [ ] **Step 1: 数值模型总表（全部 13 个，直接落码）**

| order | id | stage | hiddenFailure | base | effects → target | required | unlocks | token |
|---|---|---|---|---|---|---|---|---|
| 1 | scenario-001 | harness | hallucination | 0.08 | context-injection +0.22, tool-registry +0.05 → 0.35 | 同左 | 同左 | 1800 |
| 2 | scenario-002 | harness | tool-failure | 0.30 | tool-contract +0.15, retry-policy +0.30 → 0.75 | 同左 | tool-contract, retry-policy | 2400 |
| 3 | scenario-003 | harness | unsafe-execution | 0.40 | sandbox +0.20, permission-gate +0.10 → 0.70 | 同左 | sandbox, permission-gate | 3200 |
| 4 | scenario-004 | harness | permission-error | 0.35 | permission-gate +0.25, tool-contract +0.12 → 0.72 | permission-gate | —（复用 003） | 2600 |
| 5 | scenario-005 | harness | state-corruption | 0.30 | checkpointing +0.48 → 0.78 | checkpointing | checkpointing | 4100 |
| 6 | scenario-006 | harness | memory-failure | 0.35 | memory-management +0.39 → 0.74 | memory-management | memory-management | 5200 |
| 7 | scenario-007 | harness | context-overflow | 0.28 | context-engineering +0.48 → 0.76 | context-engineering | context-engineering | 6800 |
| 8 | scenario-008 | harness | stale-context | 0.33 | observation-loop +0.46 → 0.79 | observation-loop | observation-loop | 3900 |
| 9 | scenario-009 | loop | task-abandoned | 0.25 | recovery-loop +0.45 → 0.70 | recovery-loop | recovery-loop | 7400 |
| 10 | scenario-010 | loop | infinite-loop | 0.20 | stop-rule +0.53 → 0.73 | stop-rule | stop-rule | 9100 |
| 11 | scenario-011 | loop | false-completion | 0.15 | evidence-loop +0.70 → 0.85 | evidence-loop | evidence-loop | 5600 |
| 12 | scenario-012 | loop | budget-exhausted | 0.30 | budget-guard +0.47 → 0.77 | budget-guard | budget-guard | 12000 |
| 13 | scenario-013 | graph | deadlock | 0.10 | graph-orchestration +0.55, human-gate +0.17 → 0.82 | 同左 | graph-orchestration, human-gate | 15000 |

（`trials` 统一 200。scenario-004 复用已解锁能力、无新 unlock —— 教学点"治理是组合能力"。）

- [ ] **Step 2: 文案模板 — `scenario001.ts` 完整示例**

```ts
import type { Scenario } from './types'; // = re-export schema 类型
export const scenario001: Scenario = {
  def: { /* 上表 order=1 行 */ },
  content: {
    title: { en: 'Hallucination', zh: '幻觉' },
    mission: {
      en: 'Ask a bare model-only agent to fix a real bug in a repo it cannot see.',
      zh: '让一个只有模型的裸 Agent 修复一个它根本看不到的仓库 bug。',
    },
    failureName: { en: 'HALLUCINATION', zh: 'HALLUCINATION（幻觉）' },
    failureNarrative: {
      en: 'The agent confidently replies "bug fixed" — without reading any code or touching any file.',
      zh: 'Agent 自信地回复"已修复"——但它没有读取任何代码，也没有修改任何文件。',
    },
    missingCapabilityHint: {
      en: 'The agent has no connection to the real world. It needs grounding.',
      zh: 'Agent 与真实世界完全断开。它需要的是 Grounding。',
    },
    explanation: {
      en: 'Grounding = connecting the agent to reality. Context Injection feeds it real files; a Tool Registry tells it what actions exist. Success rate: 8% → 35%.',
      zh: 'Grounding = 让 Agent 连接真实世界。Context Injection 喂给它真实文件，Tool Registry 告诉它有哪些动作可用。成功率：8% → 35%。',
    },
    patternName: { en: 'Grounding (Context + Tools)', zh: 'Grounding（上下文 + 工具）' },
    patternSummary: {
      en: 'Never trust a model-only answer. Inject real context and register callable tools before asking for work.',
      zh: '永远不要信任纯模型的回答。让它干活之前，先注入真实上下文、注册可调用工具。',
    },
  },
};
```

- [ ] **Step 3: 其余 12 个场景文案（按设计文档逐条落码，字段同上）**

每个场景的 failureNarrative / explanation 必须覆盖设计文档对应章节的学习点：

- **002 TOOL_FAILURE**：API timeout / 参数错误 / 工具不存在 → Tool 不是函数调用，需要 Contract / Retry / Validation / Fallback。
- **003 UNSAFE_EXECUTION**：无沙箱改代码删了生产文件 → Agent 不是可信执行体，需要 Sandbox / Permission / Isolation。
- **004 PERMISSION_ERROR**：部署服务没权限 → Execution Governance：who can do what。
- **005 STATE_CORRUPTION**：多文件修改中途崩溃状态丢失 → Persistence：checkpoint / snapshot / rollback。
- **006 MEMORY_FAILURE**：长期任务记住错误信息、重复错误决策 → Memory 不是越多越好，需要 Selection / Compression / Validation。
- **007 CONTEXT_FAILURE**：长任务 context window 爆炸、重要信息丢失 → Context Engineering：什么进 prompt、什么留外面。
- **008 STALE_CONTEXT**：外部状态变了还用旧信息 → Agent 看到的世界 ≠ 真实世界，需要 Refresh / Re-fetch / Observation Loop。
- **009 TASK_ABANDONED**：修 10 个 bug 失败一次就停 → Recovery Loop（Retry Policy + Failure Recovery）。
- **010 INFINITE_LOOP_TRAP**：不断尝试-失败-再尝试 → Stop Rule：max steps / progress check / budget。
- **011 FALSE_COMPLETION ⭐**：说"改完了"但测试没过 → 根因：Agent Confidence ≠ Task Evidence → Evidence Loop：Action → Verification → Feedback → Next Action（核心实验，文案要最详细）。
- **012 BUDGET_EXHAUSTED**：无限消耗 token/时间/金钱 → Execution Control：Budget Guard / Timeout / Escalation。
- **013 DEADLOCK**：Planner→Reviewer→Executor 互相等待死锁 → Graph Engineering：Node / Edge / State Transition / Recovery Path / Human Gate。

- [ ] **Step 4: 补全 `events.ts` 的 `FAILURE_STORY` 13 条**（setup=故障前奏、failure=故障发生、mitigation=能力生效瞬间，各一句双语，与上文叙事一致，如 hallucination 的 mitigation：zh「Context Injection 生效：Agent 读取了真实文件」）。

- [ ] **Step 5: 内容完整性测试 `content.test.ts`**：`SCENARIOS.length === 13`；order 连续 1..13；每个 `requiredCapabilities` 都出现在自身或前序场景的 `unlocks` 中（可解锁性）；每个 content 字段 en/zh 均非空；`capabilityEffects` 的 key 是合法 CapabilityId。全绿，commit `feat: all 13 scenario contents (bilingual)`

### Task 11: Pattern 库 + 关卡流转

**Files:** Modify `src/pages/PatternsPage.tsx`, `src/components/StageMap.tsx`, `src/components/ExperimentShell.tsx`

- [ ] **Step 1: `PatternsPage.tsx`** — 网格展示 inventory 中已解锁能力的 `PatternCard`（从对应场景取 patternName/patternSummary）；空态文案「完成实验以收集 Pattern」。
- [ ] **Step 2: StageMap 接入真实进度** — `useProgress`：完成 ✓、可玩、锁定三态；锁定卡片点击无跳转并显示「完成前一个场景解锁」。
- [ ] **Step 3: ExperimentShell 完成后** — 显示「下一个场景」按钮（order+1 存在时），graph 全通后显示「恭喜完成全部实验」+ 跳 Pattern 库。
- [ ] **Step 4: 测试**：mock store 完成 001 后首页 002 解锁；PatternsPage 显示对应卡片。commit `feat: pattern library and progression gating`

---

## Milestone 4：打磨与上线

### Task 12: 响应式、About 页、最终验证

**Files:** Modify `src/components/*`（响应式）, Create `src/pages/AboutPage.tsx` 内容

- [ ] **Step 1: AboutPage** — 呈现设计文档核心循环（裸 Agent → 遇到失败 → 定位缺失能力 → 增加 Pattern → 重跑 → 理解 Harness/Loop/Graph）与三阶段总览图，双语。
- [ ] **Step 2: 响应式走查** — 场景页在 `md:` 断点下从双列塌缩为单列；StageMap 卡片移动端全宽；gauge 尺寸 `w-full max-w-xs`。
- [ ] **Step 3: 全量验证**

```bash
npm run test && npm run build
git add -A && git commit -m "feat: responsive polish and about page"
git push origin main
```

- [ ] **Step 4: 线上验收清单**（Actions 变绿后逐项过）：
  - 首页三阶段地图渲染，深/浅主题切换正常，刷新不 404（HashRouter）。
  - scenario-001 完整走通教学循环：基线失败 → 故障揭示 → 注入能力 → 成功率 8%→35% 动画 → 导出 Pattern → 002 解锁。
  - 切换英文后全部 UI 与场景文案为英文。
  - localStorage 进度刷新后保留。

---

## Self-Review 结论

- **Spec coverage**：设计文档 13 个场景 → Task 10 全覆盖（含 ⭐ 011 核心实验）；统一 experiment schema → Task 3 schema + Task 10 数据；核心教学循环 → Task 8 状态机；三阶段进化图 → Task 6 StageMap；metrics（successRate/tokenCost/failures）→ Task 4 + Task 9；exportPattern → Task 9 PatternCard + Task 11 Pattern 库；GitHub Pages 可预览 → Task 1 流水线 + base './' + HashRouter。
- **Placeholder scan**：场景文案 Step 3 以设计文档学习点为内容规格逐条落码，非占位符；组件实现给出结构、状态与关键代码，样式细节以 Tailwind 类约定描述。
- **Type consistency**：`ScenarioDef / ScenarioContent / CapabilityId / FailureId / TrialResult / MonteCarloSummary / RunEvent / ProgressState` 全文签名一致；`successRateOf`、`runTrial`、`runMonteCarlo`、`buildTimeline`、`useProgress`、`usePick` 名称跨 Task 一致。
